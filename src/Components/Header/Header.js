import React from 'react';
import styled from 'styled-components';

function Header() {
  return <DivHeader>Header</DivHeader>;
}

export default Header;

const DivHeader = styled.div`
  color: red;
  background-color: black;
  height: 100px;
`;
